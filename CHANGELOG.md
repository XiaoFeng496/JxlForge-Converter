# 更新日志 / Changelog

本项目所有重要改动记录于此。格式参考 [Keep a Changelog](https://keepachangelog.com/)。

## [0.2.0] - 2026-09-26

### 新增 / Added
- **EXR 挂动作时精准拦截**：为 EXR 文件启用编辑动作时，在打开图像前即被精准拦截：若 libjxl 不支持 EXR，复用上述精准提示；若 libjxl 支持但 Pillow 无法解码 EXR 像素，给出引导提示。避免泄漏不指向根因的 `cannot identify image file` 异常。
- **高位深降采样提醒**：当输入为高位深图像（16-bit / 浮点）并经软件处理降采样至 8-bit 时，状态页会明确提醒用户：高位深（HDR）数据已不可逆丢失。
- **关于页**：新增软件信息页，展示版本、引擎路径、作者（Xiaofeng496）与项目主页链接（指向 github.com/XiaoFeng496/JxlForge-Converter，支持左键拖选网址、右键复制网址）。

### 修复 / Fixed
- **非 8-bit 图像应用动作时被硬裁剪为纯白**：对 16-bit 灰度 / 32-bit 整数 / 浮点（HDR）图像施加编辑动作（调整大小、模糊、曝光等）时，原先无条件转 RGBA 会硬裁剪到 0–255，导致除 0 外几乎整图纯白。现改为先按比例归一化到 8-bit 再处理，保留正确的明暗层次。
- **RGBA 图像模糊时透明区渗色（粉色晕边）**：RGBA 图像应用模糊类动作时，透明像素中未定义的 RGB 参与卷积，在半透明边缘产生粉色/偏色晕边。现改为「预乘 → 模糊 → 反预乘」流程；全不透明图像走快路径，零额外开销。
- **EXR 转换在不支持的 libjxl 构建下报错含糊**：本机 libjxl 官方构建不含 EXR 解码器，原先转换失败只给含糊的「无法读取该输入格式」。现转换前先探测当前 libjxl 是否支持 EXR 输入，不支持即给出精准提示，不再走注定失败的编码调用。
- **打包版每次转换都提示「保持时间戳失败 … No module named 'win32timezone'」**：pywintypes 在运行时动态 import win32timezone，PyInstaller 静态分析抓不到，导致打包 exe 缺失该模块、创建时间无法保持（修改时间因走标准库仍正常）。现将其加入 hiddenimports，并在 _preserve_ctime 内用独立容错 try 收集，打包版不再告警。
- **时间戳失败提示拆分为修改时间 / 创建时间两条**：原先单一「保持时间戳失败（已忽略）」提示笼统，且修改时间成功、创建时间失败时会被整块跳过。现拆分为「保持修改时间失败（已忽略）」与「保持创建时间失败（已忽略）」，哪一步失败一目了然、互不拖累。
- **构建脚本在旧 dist 被占用时仍继续打包，可能误发旧二进制**：构建前移开旧 dist 原本只记日志不中止，当旧目录被占用（通常是上一次构建的可执行文件仍在运行、资源管理器预览窗格或杀软扫描）时仍继续跑 PyInstaller，--noconfirm 会删除同一被锁目录，可能把旧二进制当作新包发出。现改为移开失败即明确提示并退出，不再往下执行。

### 文档 / Docs
- README 明确系统要求为 Windows 10 / 11。
- README 嵌入真实界面截图（输入 / 动作 / 输出三页，覆盖简体中文、繁体中文、英文）。

## [0.1.0] - 2026-09-10

- 首个公开版本。
